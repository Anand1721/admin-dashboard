const initialState = {
    
};

const reducer = (state, { type, payload }) => {
    switch (type) {
        
        default:
            throw new Error();

    }
};

export { reducer, initialState }